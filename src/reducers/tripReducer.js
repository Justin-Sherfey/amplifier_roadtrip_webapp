const tripReducer = (state = null,action) => {
    switch(action.type){
        case 'ADD_TRIP':
            return state
        case 'REMOVE_TRIP':
            return state
        default:
            return state;
    }
}